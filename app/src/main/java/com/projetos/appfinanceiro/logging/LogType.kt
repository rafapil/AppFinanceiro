package com.projetos.appfinanceiro.logging

enum class LogType(val value: String) {
    TBS("TBS"), // Troubleshooting
    AUD("AUD"), // Audit
    SEC("SEC"), // Security
    BUS("BUS")  // Business
}